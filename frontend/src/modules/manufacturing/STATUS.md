# Status: PLACEHOLDER

This module is a **scaffold**, not an implemented feature.

**What exists:**
- `pages/ManufacturingHomePage.tsx` — placeholder landing page

**Why it exists:** The module key `manufacturing` is registered in Super Admin entitlements, so the placeholder keeps those screens working until the real module is built.

**No backend.** There is no `manufacturing` backend in `backend/src/modules/`.

**Planned for:** Phase 2 (post-MVP). Real implementation will include bill of materials (BOM), work orders, production planning, shop floor.

**Do not** add real business logic here without first creating the backend module and removing this STATUS.md.
