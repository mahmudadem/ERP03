/**
 * Domain IAccountRepository
 * 
 * Re-exports from infrastructure layer for domain access.
 */

export { 
  IAccountRepository, 
  NewAccountInput, 
  UpdateAccountInput 
} from '../../../repository/interfaces/accounting/IAccountRepository';
