/**
 * AccountRepositoryFirestore
 * 
 * Legacy export - re-exports FirestoreAccountRepository for DI compatibility.
 */

export { FirestoreAccountRepository as AccountRepositoryFirestore } from '../repositories/accounting/FirestoreAccountRepository';
