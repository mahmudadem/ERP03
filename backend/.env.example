# Database Configuration

# Database Type: FIRESTORE or SQL
DB_TYPE=FIRESTORE

# PostgreSQL Connection (only used if DB_TYPE=SQL)
DATABASE_URL="postgresql://user:password@localhost:5432/erp_enhanced?schema=public"
