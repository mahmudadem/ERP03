# Golden-Path QA Findings

Use this file during the v0.9-alpha stabilization pass.

Format each finding as one line:

`GP##-step## | Pass/Fail/Blocked | observed result | expected result | screenshot/link if available`

Example:

`GP03-step09 | Fail | invoice posted but no receipt voucher was linked | CASH FULL should create invoice + receipt vouchers | screenshot: ...`

GP01-step9: allowed if company allows , by default approvla set to flexable  when company fres starts .
GP01-step11 success with one issue , i got error message "Voucher saved as draft, but couldn't be submitted: Request failed with status code 400" instead of readble erorr.
all  GP01-steps pass
------------------
gp02-step2 not plac to set purchase or sale price , there is Price Groups area to set price groups and even this saving doesnt presist and after edting the item when clock update modal shows with empty feilds on windwos mode, some feilds on web mode.
gp02-step3: selecting service from item doesnt auto turn stock control off or disable it .
gp02-step6 : page doesnt show deferet warehouse for same item in same line but duplicates item for each WH, this is a UIUX issue , but it effects the avg cost, i am not sure but seems avg cost isnt being calculated based on current amounts in stock, i did to OP for same item the avg cost was not evaluated when 2nd OP posted, is avg cost wighted by WH?
gp02-step7 : stock movements affecting the avg cost!Avg Cost Base	!!
gp02-step8 :Stock Adjustments an doable, inputs has no labels ! and i treid to post but there was no ledger effect , also there no place to set the related accounts in inventory settins !
gp02 is un testable need alot of work !
------------------
2026-06-13 Codex GP02 retest after inventory deep-stabilization patch (tenant TESTCO / cmp_mqblxfqy_zmecyl, local emulator, suffix 130818):
GP02-step2 | Pass | item GP02-130818 saved with purchasePrice 10 and salePrice 15; service SRV-130818 was forced to trackInventory=false even when the request tried to enable stock tracking | item price metadata persists and SERVICE items stay non-stock |
GP02-step6 | Pass | Stock Levels By Item showed GP02-130818 as one item across 2 warehouses, total qty 27, blended avg cost 8.37, total value 225.99 | one item rollup with warehouse breakdown and company-wide GLOBAL average cost |
GP02-step8 | Pass after detour | Stock Adjustment OUT of 3 from warehouse G2-130818 posted voucher 4de82d76-cf9f-4627-9955-b667f6d5827a for 18 TRY even though the typed unit cost was 999 | adjustment GL uses engine cost, not user-entered override cost, and produces a readable ledger voucher |
GP02-step9 | Pass | negative-stock adjustment post was rejected with NEGATIVE_STOCK_BLOCKED; current qty 11, requested OUT 1000, negative stock disabled | OUT bigger than stock is rejected when negative stock is disabled |
GP02-global | Pass | 10 @ 5 in MAIN plus 10 @ 7 in G2 repriced both warehouses to avg 6; later 10 @ 12 in MAIN repriced both to avg 8.22; FLAT transfer posted no voucher; VALUED transfer posted voucher 8276b581-1f86-447e-9c56-d2884a8c650f for 4 TRY and both warehouses ended at avg 8.37 | GLOBAL company-wide costing reprices all locations and valued transfer capitalizes only the uplift through inventory/clearing |
GP02-reconciliation | Blocked on dirty tenant data | the new GP02 item vouchers tie, but whole-tenant Inventory GL Reconciliation is false: stock 13119.35 vs GL 346, mainly from older item 001 pre-fix drift | rerun on a fresh tenant or clean historical drift before using whole-tenant reconciliation as a pass gate |
------------------
2026-06-13 GP03 Sales (TESTCO, local emulator, driven by Claude in preview browser):
GP03-precondition | Fail→Fixed (bug) | Sales Settings page crashed blank with `TypeError: Cannot read properties of null (reading 'messagingAccounts')` at SalesSettingsPage.tsx:62 when the tenant has no saved sales settings (currentSettings unwraps to null, then .messagingAccounts is read on null). FIXED with `?? {}` null-guard on currentSettings. | page should fall back to default settings and render when no settings doc exists yet |
GP03-precondition-2 | Setup gap (not a bug) | TESTCO Sales module was never initialized — saving settings returned 403 `Module 'sales' is not initialized`, and no account mappings existed. Resolved by running the Sales Initialization Wizard (/sales when uninitialized): Operational mode, Revenue 400, inventory accounts inherited (10301/50100), 3 voucher types installed. Then set Discount 400, Refund 10101, AR Parent 10401, Warehouse MAIN in Sales Settings and saved OK. | new tenants need the sales wizard before order-to-cash; consider auto-initializing during company onboarding (GP01) |
GP03-step1a | Fail→Fixed (bug) | New customer save 500'd: `Default currency is not enabled for company: USD`. PartyMasterCard.tsx seeded form.defaultCurrency='USD' (a currency TESTCO has not enabled); the <select> visually showed the first enabled option (TRY) while state stayed USD, so USD was submitted. FIXED: loadCurrencies now defaults defaultCurrency to the company base/first-enabled currency when the seeded value isn't enabled. | new party defaults to the company base currency |
GP03-step1b | Fail (bug, not fixed) | After currency fix, save 500'd: `Party legalName is required`. The UI marks only Trading/Display Name with * and treats Legal Entity Name as optional, but the backend requires legalName. | UI should require legalName (or fall back legalName = displayName) to match backend |
GP03-step1c | Fail (bug, not fixed) | After filling legalName, save 500'd: `Generated account code already exists: 10401-CUST-1`. An earlier save that failed on the legalName validation had ALREADY created the AR sub-account 10401-CUST-1 but rolled back the party — leaving an orphan account, so retry with the same code collides. Party + AR-sub-account creation is not atomic. Worked around by using code CUST-1B. | party + auto AR sub-account creation must be atomic (one transaction) so a failed save leaves no orphan account |
GP03-step1 | Pass (with workaround) | Customer created (code CUST-1B, display CUST-1, credit limit 10000, currency TRY, auto-create AR sub-account 10401-CUST-1B). | customer saved + AR sub-account created |
GP03-step2 | Pass | Customer Statement for CUST-1 generated empty (Invoiced 0.00 / Paid 0.00 / Closing 0.00, "No transactions in this period"), no errors. | empty statement, no errors |
GP03-step3 | Blocked for automation (tooling) | Quotation: customer + item (GP02-130818) selected fine, but the ClassicLineItemsTable qty/unit-price cells do not accept programmatic input (native value-set, input/change events, and the dedicated fill tool all leave the cell empty; line total stays 0.00). The cells appear to commit only through their own keyboard handlers. Blocks automated line entry; owner drives manually from here. Suggest data-testid hooks on cells. | line items entered; quote saved |
GP03-step3b | Fail→Fixed (bug, found via owner manual entry) | With line 1 fully filled, saving the quote errored "Line 2: item is required" — QuotationDetailPage.validate() looped over ALL form.lines including the table's trailing empty working row. FIXED: validate() and buildPayload() now filter to filled lines only (isFilledLine = itemId/itemCode/itemName/description), matching the table's isRowFilled. NOTE: sibling document pages (SO/DN/SI/SR/PO/PI/PR) likely share this validate-all-lines pattern — check each. | empty trailing row ignored on save |
GP03-step3 | Pass | Quote QT-00001 saved: CUST-1, 10 x GP02-130818 @ 15, Grand Total 150.00, DRAFT. | quote saved with number |
GP03-step4 | Pass | Quote → Send → Accept → Convert to Sales Order produced SO-00001, CONFIRMED, 150.00; credit check passed (150 « 10000 limit). | SO confirmed, credit check passes |
GP03-step5 | Pass | Delivery Note created from SO (Deliver Goods), posted; GP02-130818 stock dropped 27→17 (-10); NO GL voucher at DN (correct for invoice-driven mode). | stock drops, COGS deferred per invoice-driven policy |
GP03-step6 | Pass (note) | SI created from SO-00001; source auto-detected as FROM DN (invoicing delivered qty linked to DN-00001) — correct SO→DN→Invoice chain. NOTE: standalone "pick a DN directly" picker on the SI page is not wired (code warns "Delivery Note source loading is not connected"); DN-linked invoicing works via the SO path. Lines inherited 10 @ 15. | lines inherited, qty/price correct |
GP03-step7 | Pass | Line discount 10% → line total 150→135. | taxable base reduces |
GP03-step8 | Pass | Add Charge Freight 50 + Add Discount 20 → Grand Total 165.00 (135+50-20). | grand total correct |
GP03-step9-10 | Pass | SI-00001 POSTED+PAID, 165.00. 3 vouchers: SI-SI-00001 invoice (200 dr=AR165+discounts35 / cr=revenue150+freight50, balanced); SI-COGS-SI-00001 COGS→Finished Goods 83.70 (=10×8.37 GLOBAL company avg cost — GLOBAL engine feeds sales COGS correctly); RV-0001 receipt Cash→AR 165 (clears AR → PAID). COGS recognized at invoice (not DN). | balanced GL, two/three vouchers, COGS+inventory consistent |
GP03-step11 | Pass | Payment History on SI-00001 shows CASH 165.00 with linked voucher. | payment history shows the receipt |
GP03-step14a | Fail→Fixed (bug) | Posting a Sales Return (After Invoice, 2 units) of an invoice line that had a discount errored: `Subledger voucher SR-REV-SR-00001 is not balanced: debit=30, credit=27`. SalesReturnUseCases posting recompute (lines ~693-697) computed the revenue reversal from GROSS (qty×unitPrice) and only stripped tax — it never applied the inherited line discount, while the AR credit was net-of-discount → off by the discount (3 = 2×1.50). FIXED: apply the line discount before tax in the recompute, mirroring SalesInvoice.calculateDiscountAmountDoc. Backend rebuilt; re-post succeeded. | return reversal balanced when source line had a discount |
GP03-step14 | Pass (after fix) | SR-00001 posted. SR-REV: Dr Sales Revenue 27 / Cr AR 27 (balanced, net of 10% discount). SR-COGS: Dr Finished Goods 16.74 / Cr COGS 16.74 (=2×8.37 GLOBAL avg). Stock GP02-130818 17→19 (+2). | stock +2, COGS reversed, AR credit-note balanced |
GP03-step15 | Pass | Customer Statement CUST-1: INVOICE SI-00001 +165 → 165; PAYMENT RV-0001 -165 → 0; CREDIT_NOTE SR-00001 -27 → -27. Closing -27.00 (Invoiced 165 / Paid 165 / Credits 27). | invoices, receipt, return shown; running balance ties to AR |
GP03-step16 | Pass | AR Aging: CUST-1 Total AR -27.00, matching the statement closing balance. | AR outstanding equals statement balance |
GP03-step17 | Pass | Trial Balance balanced: GRAND TOTAL 1,426.04 = 1,426.04. Sales Revenue 138.00 = 150 line +50 freight -15 line-disc -20 inv-disc -27 return. | trial balance balanced |
GP03-observation | Opening-stock GL (pre-existing, not GP03) | Trial Balance shows COGS (501) with a credit balance because TESTCO opening-stock vouchers booked Dr Inventory / Cr Cost of Goods Sold. Opening inventory should offset to an Opening Balance Equity (or Retained Earnings) account, not COGS — crediting COGS understates cost of sales. Inventory-module opening-stock posting concern; revisit during inventory GL hardening. | opening stock offsets to opening-balance equity, not COGS |
---
GP03-step12-13 | Blocked by design + bug | Direct sales invoice (persona 'direct') is blocked in OPERATIONAL workflow by DocumentPolicyResolver base policy — so steps 12-13 "direct SI" cannot post in this tenant. Confusing because the setup wizard shows "Allow Direct Invoicing: Yes" and Sales Settings has allowDirectInvoicing — but the persona base policy for OPERATIONAL overrides it. (1) UX inconsistency: wizard/setting imply direct is allowed while Operational base policy forbids it. (2) BUG (owner-found): trying to switch to SIMPLE (to enable direct) is refused "Cannot switch to Simple workflow while there are draft or posted delivery notes" — FirestoreDeliveryNoteRepository.hasUnpostedDeliveryNotes() queries status in ['DRAFT','POSTED'], so a fully-invoiced POSTED DN blocks the switch forever (DN has no INVOICED/COMPLETED status). Inconsistent with Purchases (hasUnpostedGoodsReceipts blocks drafts only) and contradicts the method name "unposted". Fix: block on DRAFT only (or track invoiced coverage). Workaround for 12-13: invoice from an SO (linked persona) instead of direct — same settlement logic. | direct invoicing usable, or guard not blocking on completed DNs |
GP03-step12c | Bug (UX, found via owner) | "Create Invoice" from a CONFIRMED-but-undelivered Sales Order opens a Sales Invoice with NO line items (Operational mode invoices delivered qty only; an undelivered SO has nothing invoiceable). Should show a clear "nothing to invoice — deliver the goods first" message instead of silently creating an empty invoice. (SO-00001 worked because it was delivered.) | warn when SO has no invoiceable/delivered qty |
GP03-step12d | Bug (misleading setting — owner insight: "DN should be optional") | DN being mandatory in Operational is configurable, but the control is hidden. DocumentPolicyResolver.getSalesInvoiceBasePolicy: allowDirectInvoicing is DEPRECATED in OPERATIONAL (comment: "governance rules are the only valid way to re-enable direct invoicing"); base policy OPERATIONAL = {direct:false}. Yet the Sales setup wizard shows "Allow Direct Invoicing: Yes" and Sales Settings → Sales Policy shows the allowDirectInvoicing checkbox — both DEAD in Operational. The real lever is Sales Settings → Governance tab (add rule scope=company/persona=direct/action=allow). FIX: in Operational, either hide/relabel the dead allowDirectInvoicing toggle, or wire it to create the governance 'allow direct' rule, so DN is genuinely optional via one obvious control. Agreed with owner: a business should be able to invoice without a DN. | one clear toggle makes direct invoicing (DN-optional) work in Operational |
GP03-step3-sibling | Fail→Fixed (bug) | The empty-trailing-row validation bug (GP03-step3b) confirmed on SalesOrderDetailPage too ("Line 2: item is required" on SO save). FIXED the same way (filter to isFilledLine in validateBeforeSave + payload). DN/SI/SR/PO/PI/PR pages still to check. | trailing empty row ignored on SO save |
GP03-step12 | Pass (both halves) | (a) Flag OFF: paying 1500 on a 1000 invoice rejected "Over-payment exceeds allowance." (b) Flag ON: SI-00002 (200) paid 400 → invoice PAID + 200 over-payment parked as customer credit (statement closing -227 = -27 return + -200 overpay). | over-payment ON → PAID + credit; OFF → rejected |
GP03-step13a | Fail→Fixed (bug) | Record Payment failed: "receivablePayableAccountId is required or Sales default AR account must be configured". PaymentSyncUseCases (PostSalesInvoiceWithSettlement / RecordSalesInvoicePayment) resolved AR only from settings.defaultARAccountId, never the customer's own AR sub-account — unlike post-time settlement (SalesInvoiceUseCases.resolveARAccount uses customer.defaultARAccountId). The UI sets only arParentAccountId, so defaultARAccountId is empty → Record Payment broken on every freshly-set-up tenant. FIXED: added partyRepo to both use cases + controller wiring; resolve customer.defaultARAccountId before settings fallback. | record payment resolves the customer AR sub-account |
GP03-step13b | Fail→Fixed (bug) | After the AR fix, Record Payment of a partial amount errored "CASH_FULL settlement total (555) must equal outstanding amount (1110)". The record-payment endpoint defaulted settlementMode to CASH_FULL (forces full payment), but the RecordPaymentDialog is built for partial payments and sends no mode. FIXED: SalesController record-payment default → MULTI (handles partial→PARTIALLY_PAID, full→PAID, over→credit). | partial record-payment allowed |
GP03-step13 | Pass (after fixes) | SI-00003 (1110) partial payment 555 → PARTIALLY_PAID; receipt linked; AR 883→328. Customer statement closing 328.00 (Invoiced 1475 / Paid 1120 / Credits 27). | partial paid, receipt linked, AR reduced by half |
---
GP03 SUMMARY (2026-06-13): ALL 17 steps PASS. Full SO→DN→Invoice→COGS→Receipt→Return + over-payment + partial pay-later chain posts balanced GL with GLOBAL-costed COGS (8.37); trial balance balances.
Bugs found: 12 + 1 observation.
  FIXED (7): (1) Sales Settings page crash on null settings; (2) new-party currency defaults to USD→500; (3) Quote empty-trailing-row validation; (4) Sales Return discount-reversal imbalance; (5) Sales Order empty-trailing-row validation; (6) Record-Payment didn't resolve customer AR sub-account; (7) Record-Payment defaulted to CASH_FULL (blocked partial pay).
  LOGGED, NOT FIXED (5): (8) backend requires legalName while UI marks it optional; (9) party + AR-sub-account creation not atomic (orphan account on failed save); (10) "Allow Direct Invoicing" toggle/wizard is dead in Operational — direct invoicing only via Governance tab (misleading; DN should be optionally skippable via one clear control); (11) Create-Invoice from an undelivered SO silently makes an empty invoice (should warn); (12) Simple-workflow switch blocked by POSTED (not just draft) DNs — inconsistent with Purchases, contradicts method name.
  OBSERVATION: opening-stock vouchers offset to COGS instead of Opening Balance Equity (inventory GL concern).
  REMAINING CHECK: the empty-trailing-row validation pattern (#3/#5) likely also affects DN/SI/SR/PO/PI/PR pages.
------------------
2026-06-13/14 GP04 Purchases (TESTCO, local emulator, Claude driving):
GP04-precondition-1 | Fail→Fixed (bug, blocker) | Purchases setup wizard could NOT be completed on an invoice-driven + OPERATIONAL tenant. PurchaseInitializationWizard stepError required defaultGRNIAccountId for any OPERATIONAL workflow (line 146), but the GRNI field only renders in PERPETUAL mode (line 385) — so the required field was never shown → deadlock. FIXED: gate the requirement on accountingMode === 'PERPETUAL' (+ added accountingMode to memo deps). | invoice-driven + Operational tenants can complete the purchases wizard |
GP04-precondition-2 | Fail→Fixed (bug, blocker) | After the validation fix, initialize POST 400'd: "defaultGRNIAccountId is required". The wizard sent defaultGRNIAccountId: '' (empty string) for invoice-driven OPERATIONAL; the backend's ensureOptionalString rejects a present-but-empty string. FIXED: wizard payload now coerces '' → undefined (defaultGRNIAccountId only sent when actually set). Purchases module then initialized OK (Operational, AP 20100, expense 50101). | empty optional account omitted, not sent as '' |
GP04-step1 | Pass | Vendor VEND-1 created cleanly first try (currency defaults TRY, legalName filled, Auto-create AP sub-account 20100-VEND-1). GP03 fixes (#2 currency) carried over. | vendor saved + AP sub-account |
GP04-step2 | Pass | Vendor Statement VEND-1 empty (Billed 0 / Paid 0 / Closing 0), no errors. | empty, no errors |
GP04-step3a | Fail→Fixed (bug) | PurchaseOrderDetailPage had the same empty-trailing-row validation bug (validateBeforeSave + payload iterate all lines). Fixed proactively (filter to isFilledLine). | empty trailing row ignored on PO save |
GP04-step3 | Pass | PO-00001 VEND-1 TRY 500.00 CONFIRMED (50 × GP02-130818 @ 10). | PO confirmed |
GP04-step4 | Pass | GRN-00001 posted from PO; stock GP02-130818 +50 (PURCHASE_RECEIPT IN 50, valued 500). NO GL at GRN (invoice-driven — like the sales DN; GRNI deferred to PI). Stock fully traceable: all 13 movement rows for the item net to exactly 58 = stock level. | stock +50, GL deferred per invoice-driven policy |
GP04-step4-uxbug | Bug (UX, owner-spotted) | On the GRN (and likely PI), the "PO REFERENCE" field shows the raw PO UUID (e.g. 95815d04-5a47-...) instead of the human-readable PO number (PO-00001). Resolve the id to the document number for display. | show PO-00001, not the UUID |
GP04-step5to8a | Fail→Fixed (BUG — money impact, the most significant of the run) | Purchase Invoice posting DROPPED the line discount from the GL. PI-00001 line showed net 475 (5% discount) on the form, but the posted journal was: Dr Finished Goods 500 (GROSS), Dr Freight 30, Cr Freight 10 (inv discount), Cr AP 520. AP should be 495 (475 net + 30 − 10) — vendor OVER-CREDITED by exactly the 25 line discount; inventory also overstated by 25. Root cause: PurchaseInvoiceUseCases.freezeTaxSnapshotSync recomputed lineTotal from gross (qty×unitPrice)/taxDivisor at posting time, never subtracting the line discount (same class as the GP03 sales-return bug). FIXED: apply the line discount before tax in freezeTaxSnapshotSync (mirrors SalesInvoice.calculateDiscountAmountDoc); backend rebuilt. NOTE: PI-00001 was posted BEFORE the fix and still carries AP 520 — must be reversed+reposted (or excluded) before vendor statement / AP checks are trusted. | line discount reduces inventory + AP; vendor owed the net |
GP04-step5to8b | Pass | Purchase charge/discount FLIP confirmed correct in the same voucher: charge (Freight 30) DEBITS, invoice discount (10) CREDITS — opposite of sales; voucher balanced 530=530. (Minor: invoice discount credited the charge's account 50102 since no dedicated purchase-discount account is mapped.) | charge debits, discount credits |
GP04-step5to8-verify | Fix verified (unit test) | Added PurchasePostingUseCases.test.ts case 6c: 50 × 10 with 5% line discount → asserts inventory debit = NET 475 (and explicitly NOT gross 500) and AP credit = 475, balanced. 16/16 PI posting tests pass. The line-discount posting fix is confirmed correct + regression-covered. | line discount nets inventory + AP |
GP04-step9-blocker | Fail→Fixed+verified (bug) | "Unpost Invoice" 409'd: INFRA_005 "Firestore transactions require all reads to be executed before all writes." Root cause: UnpostPurchaseInvoiceUseCase ran voucher-delete (deleteForVoucher = ledger query READ → delete WRITE) and inventory reversal (RecordStockMovementUseCase.deleteMovement = getOrCreateStockLevel READ → upsert/delete WRITE) in ONE transaction, so the inventory READ landed after the voucher WRITE. FIXED by splitting the unpost into separate transactions (voucher; then each movement reversal in its own txn via deleteMovement without a shared txn; then PO/PI writes), each internally read-before-write — mirrors how POST pre-separates reads/writes; ordering is retry-safe. VERIFIED LIVE: PI-00001 unposted POSTED→DRAFT, re-posted clean. Full purchases+inventory suites green (19 suites/121 tests). | unpost reverses cleanly |
GP04-step5to8-LIVE-verify | Pass (live) | After unpost + re-post with the line-discount fix, PI-00001 voucher dropped 530→505 and the Vendor Statement shows AP/Billed/Closing = 495.00 (was 520) — the 25 line discount now correctly reduces inventory (475) and AP (495). Both the line-discount fix and the unpost fix confirmed end-to-end in the running emulator. | vendor owed the net (495) |
---
GP04 STATUS (2026-06-14): Steps 1-8 PASS (with fixes). Steps 9-14 NOT run live — blocked because PI-00001 was posted before the line-discount fix (AP 520) and the Unpost feature is itself broken (txn read/write ordering), so it can't be cleanly corrected in-place. Bugs found: 6 — FIXED+verified (5): wizard GRNI deadlock, wizard empty-string GRNI, PO empty-row, PI empty-row, PI line-discount-dropped-from-GL (unit-tested 6c). LOGGED not fixed (1 + 2 minor): Unpost txn read/write ordering (blocker); PO-ref shows raw UUID; invoice discount lacks a dedicated purchase-discount account. Next: fix Unpost (reads-before-writes), then redo PI-00001 and finish steps 9-14; also the empty-row pattern likely affects PR/GRN pages.
